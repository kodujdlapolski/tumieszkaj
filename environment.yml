# run: conda env create --file environment.yml

name: tumieszkaj
channels:
  - conda-forge
dependencies:
  - python=3.7
  - pandas
  - jupyterlab
  - pip
  - matplotlib
  - shapely
  - tqdm
  - pip:
      - anytree
      - tables
      - scrapy
      - geopy
      - OSMPythonTools
      - overpy
      - gtfstk
