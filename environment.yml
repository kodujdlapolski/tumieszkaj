# run: conda env create --file environment.yml

name: tumieszkaj-private
channels:
  - conda-forge
dependencies:
  - python=3.7
  - pandas
  - jupyterlab
  - pip
  - matplotlib
  - shapely
  - requests
  - tqdm
  - pip:
      - anytree
      - tables
      - scrapy
      - geopy
      - OSMPythonTools
      - overpy
      - jsonlines
      - gtfstk
